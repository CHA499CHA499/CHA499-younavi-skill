# JMQS Driver Skill

可安装的使用说明位于 [`../../skills/jmqs-driver/SKILL.md`](../../skills/jmqs-driver/SKILL.md)。

从仓库根目录安装/使用该 Skill 后，项目入口是本目录；先执行 `npm install`，再执行 `npm run dev:persona`，访问 `http://localhost:3000`。
